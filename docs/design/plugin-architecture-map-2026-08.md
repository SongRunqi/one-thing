# 插件系统全景架构图(extension surface map,2026-08-09)

> 起因:用户提出方法论转向 —— 不再"场景驱动、一砖一砖凿墙",而是先画出
> 整座房子的图纸,再按图施工。本文的方法:**场景枚举不完,宿主枚举得完**。
> 插件系统的能力上限 = 宿主自身"名词与动词"的系统投影(VS Code 的
> contribution points 同理:不是从场景推的,是编辑器域模型的投影)。
> 于是把宿主的全部子系统盘一遍(`shared/ipc` 36 个域 + core/runtime 注册表),
> 每一间屋子给出裁决:**已建 ✅ / 规划 🔷 / 暂缓 ⏸ / 永不 ⛔**。
> 此后任何插件想法都是**查表**,不是立项。

## 0. 房子的骨架:任何插件 = 四个象限

```
        感知(它能看见什么)          动作(它能做什么)
        events / 只读快照            tools / commands / 动词族 API
   ┌─────────────────────┬─────────────────────┐
   │                     │                     │
   │      呈现(它长什么样)│   资产(它拥有什么)   │
   │  锚点/面板/外观/氛围  │   config/kv/消息态/包内资产 │
   └─────────────────────┴─────────────────────┘
```

**"写插件不用先给系统加能力"的判据**:四个象限各自覆盖得足够宽。
建设顺序也由此而来(§8):先补感知与动作的洞(它们决定"能不能做到"),
再补呈现的洞(决定"好不好看"),最后是外部触达。

## 1. AI 能力面(模型侧)—— 最成熟的一翼

| 屋子 | 状态 | 形态 |
| --- | --- | --- |
| 工具(tools) | ✅ | `api.registerTool`,进模型工具表,走权限/熔断 |
| 斜杠命令 | ✅ | `api.registerCommand`(须同步 core 注册表) |
| **系统提示注入** | ✅ **(用户以为没有)** | `prompt-context providers` —— builder 每次组装收插件贡献(`prompts/builder.ts` 的 `collectPluginPromptContext`) |
| 事件订阅 | ✅ | `api.on`(会话/流/步骤事件 + 插件自定义事件) |
| 生命周期钩子(观察型) | ✅ | 仅 2 个:`beforeContextCompact` / `afterAssistantResponse` —— 带全上下文的**通知点**,返回值被忽略,不能拦不能改;5s 预算+熔断 |
| **拦截钩子(干预型)** | ⏸ | 内联关键路径,风险另一等级 —— 按点解冻,候选序:`beforeSend`(改写待发消息)→ `beforeToolCall`(审改参数)→ `beforeProviderRequest`;每点单独设计预算/超时回退(=当没改)/次序/幂等 |
| 技能根 | ✅ | `api.registerSkillRoot` |
| 调度器 | ✅ | `api.scheduler`(定时任务) |
| 变量系统 | ⏸ | 变量是运行时状态面板;插件写变量 = 污染模型上下文的口子,等真实需求 |
| 自定义 provider(接新模型) | ⏸ | ProviderDefinition 是重接口(流/工具/思维档位);错误实现直接坏会话。等生态 |
| Agent 人设/协作面 | ⏸ | 协作域模型仍在演进(Actor v3),稳定前不冻结对外契约 |

## 2. UI 位置面(在哪出现)

| 屋子 | 状态 | 形态 |
| --- | --- | --- |
| 工作区面板 | ✅ | 描述树/webview 双形态 |
| 常显锚点 ×3 | ✅ | composer.above(含**抽屉三态**)、chat.status-bar(chip 壳)、message.footer |
| 触发锚点 ×2 | ✅ | message.actions(⋯ 菜单)、composer.actions(工具条钮),入口宿主画、弹层按需 |
| workbench 标签页 | 🔷 H1 | tab 联合加 plugin 变体,面板双形态复用 |
| 设置页(插件自己的配置) | ✅ | R3 schema 驱动,宿主渲染 |
| 独立设置 tab / sidebar rail / 消息正文内嵌 | ⛔ | 审计红线(归口已有 / 语义固定 / 历史污染) |

## 3. UI 内容面(长什么样)

| 屋子 | 状态 | 形态 |
| --- | --- | --- |
| 描述树 v2 | ✅ | 16 节点 + 11 控件,含 data: 图(自绘 SVG 通路) |
| webview | ✅ | sandbox iframe + `onething-plugin://`,面板住址 |
| 围栏渲染器 | 🔷 H2 | 接管特定 ``` 围栏的**显示**(display-only,历史永存纯文本) |
| 通用 markdown 改写 / DOM 注入 | ⛔ | 宪法红线 |

## 4. 外观面(什么风格)

| 屋子 | 状态 | 形态 |
| --- | --- | --- |
| 主题 token 覆盖 | ✅ | L2,合成在派生层之前(染遍 --ui-*) |
| 背景层 | ✅ | L2.5,包内资产 + 钳制 + 运行期调参 |
| 氛围层(全窗动画覆盖) | 🔷 G2 已设计 | webview 第三住址 + 几何喂送(pointer-events:none,z 在浮层下) |
| 皮肤包(composer/气泡/代码块档位) | 🔷 H3 | 枚举结构档位 + highlights 主题族放开 |
| 任意 CSS | ⛔ | 永不 |

## 5. 数据面(它拥有什么)

| 屋子 | 状态 | 形态 |
| --- | --- | --- |
| config / kv / storage 目录 | ✅ | 家目录 `plugins/<id>/`,卸载归档 |
| 消息态 | ✅ | `api.storage.message(sid, mid)`,宿主管坐标(级联/配额/水合),lifetime 声明开闸 |
| 会话态 | ⏸(刻意空格) | 布局留了位,第一个真实住户出现才建 |
| 包内静态资产 | ✅ | `onething-plugin://` 协议(webview/背景共用) |

## 6. 宿主动词面(它能替用户做什么)—— 本图新开的一翼

这一翼是用户举例("session rename 能不能是插件"、"提示音"、"search
everywhere 集成")暴露的系统性缺口:插件的"动作"象限目前只有
**面向模型**的动词(工具),几乎没有**面向宿主**的动词。逐屋裁决:

> 2026-08-10 更新:N1 已落地(跨会话信使 + 会话感知快照),这一翼不再是空的。
> 设计与差异见 `pi-benchmark-adoption-2026-08.md` §6。

| 屋子 | 状态 | 形态与闸 |
| --- | --- | --- |
| **通知与提示音** | 🔷 M1 | 现有 `ui.notify`(静默横幅);扩为 `api.notify({title, body, sound?})`,sound 是**宿主枚举音效集**(插件不带音频文件,防音频轰炸);设置页每插件可静音 |
| **Search Everywhere 供给方** | 🔷 M2 | `api.registerSearchProvider({id, label, query(q) → 可序列化结果[]})`,搜索窗聚合渲染(结果形状=宿主枚举:标题/副标/图标/action);超时即弃(搜索不等慢插件) |
| **跨会话信使** | ✅ N1 | `api.sendMessage(sessionId, content, {triggerTurn} \| {deliverAs})` —— **三态投递矩阵**(空闲起轮 / 忙则降级为 steer / 只入队不起轮),映射到既有 steering / follow-up / command:send-message 三条链路,不造新队列。声明门 `sessions:post` + `sessions:trigger`(装前逐条披露);循环闸 = 跳数上限 8 + 每(插件,会话)对 10 次/分钟;拒绝一律回结构化 reason。**这是第一个"插件可自发耗 token"的口** |
| **会话感知快照** | ✅ N1 | `api.sessions.peek(id)` → `{state, currentTool?, lastMessage(≤120 字符), contextPercent?, title, updatedAt}` / `api.sessions.list()`(peek-lite,无正文)/ `api.isIdle(id)`。声明门 `sessions:peek`。**压缩快照动词,不是事件流**:agent 想看才调;全部从现成内存态现算,零新统计,正文永不整条出境 |
| **会话写动词族(rename / archive)** | 🔷 M3 余量 | `api.sessions.*` 只开**枚举白名单**:`rename(id, title)`/`archive(id)`;每个写动词走权限面(与工具同规,用户可设 always-ask);**不开** delete(破坏性)与消息级写(历史神圣)。`list()` 已由 N1 落地 |
| 剪贴板 / 文件系统 / shell | ⛔(直接) | 走既有工具面(bash/read/write 有权限体系),不给插件裸 API —— 同一动作一套权限账 |
| 设置写入(改宿主设置) | ⛔ | 插件只管自己的 config;改宿主设置=用户主权 |
| 窗口管理(开新窗) | ⏸ | 等 workbench.tab 落地后看剩余需求 |

## 7. 外部触达面(从哪进来)与生命周期面

| 屋子 | 状态 | 形态 |
| --- | --- | --- |
| 深链/自动化 | 🔷 H4 | `onething://plugin/<id>/<action>` + 同意闸(PopClip 类) |
| IM connector | ✅(试点休眠) | 注册面已开,无生产流量 |
| server/web 执行插件 | ⏸ | 方案 A 已拍板;评估已登记(分发设计 §12.0) |
| npm 市场/更新/回滚/披露/熔断/拆除 | ✅ | R 线 + P 线全套 |
| 子进程 ext host | ⏸ H 线终局 | 宪法第 2 条持续兑现中,随时可换 |

## 8. 施工顺序(按结构完备度,不按场景)

原则:先把"能不能做到"的洞补完(感知+动作),再补"好不好看"的洞。
一个象限补完,落进该象限的**所有**未来场景自动获得支持。

```
第零批(已完成 2026-08-10):N1 跨会话信使 + 会话感知快照 —— 动作象限里
                            "面向宿主的动词"从零变成一;感知象限补上压缩快照
第一批(动作完备,M 线):M1 通知音 → M2 搜索供给方 → M3 会话写动词(rename/archive)
第二批(呈现完备):   G2 氛围层(已设计) → H1 workbench.tab → H3 皮肤包
第三批(触达完备):   H4 深链 → H2 围栏渲染器(最重殿后)
持续:              H 线 ext host(结构条件已备,按需启动)
```

M 线插队到最前的理由:它是用户例子暴露的**象限级空洞**(宿主动词面
几乎为零),而 G2/H 系列只是已有象限里的增量房间。

## 9. 使用方式(给未来的自己)

新想法来了 → 拆成四象限 → 逐象限查本表:
- 全 ✅ → 直接写插件,零宿主改动;
- 有 🔷 → 该房间的设计已在案,提前施工即可;
- 有 ⏸ → 看这次是不是"第一个真实住户",是则解冻;
- 撞 ⛔ → 找同象限的合法邻居(表里每条 ⛔ 都写了替代出口)。
本表是**单一事实源**:开新屋必须先改本表(与锚点治理同规,append-only)。
