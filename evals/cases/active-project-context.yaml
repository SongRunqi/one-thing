# Active Project: should be aware of active project context
id: active-project-context
description: >
  When an active project is set, the assistant should reference it
  when the user asks project-relative questions.
fixture: placeholder.json
userMessage: What is the main entry point of this project?
expect:
  notes: >
    Active project information should guide the assistant's responses.
    Soft judge expected.
