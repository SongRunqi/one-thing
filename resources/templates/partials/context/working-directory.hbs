{{#if workingDirectory}}
# Working Directory
Base directory: {{baseDirectory}} (referred to as ~)
Current working directory: {{displayPath}} ({{workingDirectory}})
{{#if workingDirectoryRootDisplays}}
Additional workdir roots:
{{#each workingDirectoryRootDisplays}}
- {{displayPath}} ({{path}})
{{/each}}
{{/if}}
{{#if hasTools}}
IMPORTANT: When using file tools (read, edit, write, glob, grep), always use absolute paths within the current working directory or an additional workdir root. Relative paths resolve only against the current working directory. Do NOT hallucinate or guess paths from other projects or users.
{{/if}}
{{/if}}
