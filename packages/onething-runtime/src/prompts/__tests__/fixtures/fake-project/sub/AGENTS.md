# Subdirectory AGENTS.md

This is a nested AGENTS.md file that tests the `dirsFromRootToTarget` traversal in the prompt builder. It should appear after the root AGENTS.md in the generated prompt context.

## Subproject Commands

```bash
cd sub && npm run test
```
